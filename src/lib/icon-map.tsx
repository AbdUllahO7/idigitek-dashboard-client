import { Clock, MessageSquare, LineChart, Headphones, Car, MonitorSmartphone, Settings, CreditCard } from "lucide-react"

// Icons mapping for dynamic rendering
export const IconMap = {
  Clock,
  MessageSquare,
  LineChart,
  Headphones,
  Car,
  MonitorSmartphone,
  Settings,
  CreditCard,
}
