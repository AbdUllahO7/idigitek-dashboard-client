import { PageLoader } from "@/src/components/ui/loader";

/**
 * Sign-in loading component
 * Shown during sign-in page load
 */
export default function SignInLoading() {
  return <PageLoader text="Loading sign-in..." />
}
