import { PageLoader } from "@/src/components/ui/loader";

/**
 * Dashboard loading component
 * Shown during dashboard page load
 */
export default function DashboardLoading() {
  return <PageLoader text="Loading dashboard..." />
}
